### Getting started
* clone repo
* composer install
* cp .env.example .env and set config settings for db
* artisan key:generate
* artisan migrate
* artisan db:seed

front End
* npm install
* npm run dev (or prod)